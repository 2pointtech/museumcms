<%= form_for @classroom_schedule do |f| %>
  <%= render :partial => 'form', :object => f %>
  <div class="form-actions">
    <button type="submit" class="btn btn-primary">Create</button>
  </div>
<% end %>
